# RazorShield AI — Python Backend Requirements
# ─────────────────────────────────────────────────────────────────────────────
# Install on macOS (Homebrew Python 3.11):
#   python3.11 -m pip install torch --index-url https://download.pytorch.org/whl/cpu
#   python3.11 -m pip install -r razorshield_backend/requirements.txt
#   python3.11 -m playwright install chromium
#
# Start server:
#   python3.11 -m uvicorn razorshield_backend.main:app --reload --port 8000
# ─────────────────────────────────────────────────────────────────────────────

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Config & Validation ---
pydantic>=2.9.0
pydantic-settings>=2.5.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pgvector>=0.3.0

# --- HTTP & Async ---
httpx>=0.27.0
anyio>=4.6.0

# --- Scraping ---
playwright>=1.47.0
beautifulsoup4>=4.12.0
lxml>=5.3.0

# --- Domain Intelligence ---
python-whois>=0.9.4
tldextract>=5.1.0

# --- LLM Orchestration ---
litellm>=1.90.0

# --- Local Embeddings (768-dim, free, no API key needed) ---
# torch must be installed FIRST with CPU-only index:
#   python3.11 -m pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=3.1.0

# --- Data & Evaluation ---
scikit-learn>=1.5.0
pandas>=2.2.0
numpy>=1.26.0

# --- Utilities ---
python-dotenv>=1.0.0
python-multipart>=0.0.12
