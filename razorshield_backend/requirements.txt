# RazorShield AI — Python Backend Requirements
# Install: pip install -r requirements.txt
# For CPU-only PyTorch (saves ~2GB): pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then: pip install sentence-transformers

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Config & Validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pgvector==0.3.5

# --- HTTP & Async ---
httpx==0.27.2
anyio==4.6.0

# --- Scraping ---
playwright==1.47.0
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Domain Intelligence ---
python-whois==0.9.4
tldextract==5.1.2

# --- LLM Orchestration ---
litellm==1.48.7

# --- Local Embeddings (768-dim, free, no API key needed) ---
sentence-transformers==3.1.1
# torch is a transitive dep — install CPU-only separately for size:
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Data & Evaluation ---
scikit-learn==1.5.2
pandas==2.2.3
numpy==1.26.4

# --- Utilities ---
python-dotenv==1.0.1
python-multipart==0.0.12
